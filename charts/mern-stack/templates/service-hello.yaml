apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.helloService.name }}-svc
  namespace: default
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: {{ .Values.helloService.containerPort }}
      protocol: TCP
  selector:
    app: {{ .Values.helloService.name }}
