apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.profileService.name }}-svc
  namespace: default
spec:
  type: ClusterIP
  ports:
    - port: 3002
      targetPort: {{ .Values.profileService.containerPort }}
      protocol: TCP
  selector:
    app: {{ .Values.profileService.name }}
