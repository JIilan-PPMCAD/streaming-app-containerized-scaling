apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.frontend.name }}-svc
  namespace: default
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: {{ .Values.frontend.containerPort }}
      protocol: TCP
  selector:
    app: {{ .Values.frontend.name }}
